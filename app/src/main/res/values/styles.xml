<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:fontFamily">@font/copperplate_font_family</item>
    </style>

    <style name="AppTheme.Splash">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="GridItem">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:foreground">?selectableItemBackground</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintStart_toStartOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
        <item name="layout_constraintDimensionRatio">H,1:1.5</item>
    </style>

    <style name="BottomNavigationViewTextStyleActive">]
        <item name="android:fontFamily">@font/copperplate</item>
        <item name="android:textSize">10sp</item>
    </style>

    <style name="BottomNavigationViewTextStyleInactive">]
        <item name="android:fontFamily">@font/copperplate</item>
        <item name="android:textSize">8sp</item>
    </style>
</resources>
